enum SORT_TYPES{
    ASC = "asc",
    DESC = "desc",
    HIGHER = "higher",
    LOWER = "lower",
}

enum FILTER_TYPES{
    SEARCH = "search",
    ORDER = "order",
    PAGE = "page"
}

export {FILTER_TYPES, SORT_TYPES};